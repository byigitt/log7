# Phase 1: Project Setup

## Commit: `feat: initial project setup`

## What to do
- Create `package.json` with pnpm, discord.js, mongoose, dotenv, typescript
- Create `tsconfig.json` for TypeScript compilation
- Create `.env.example` with DISCORD_TOKEN, CLIENT_ID, MONGODB_URI
- Create `.gitignore` to ignore node_modules, dist, .env, etc.
- Create empty directory structure for src/

## Directory Structure
```
src/
  config/
  types/
  database/models/
  database/services/
  events/handlers/
  commands/logging/
  utils/
  constants/
```
